# Phase 2: Canvas OAuth 2.0 routes will go here.
#
# In phase 2, login/connect/callback/logout endpoints will be added and
# CanvasClient will receive the token from the authenticated User record
# instead of from config — a one-line change in canvas_client.py.
